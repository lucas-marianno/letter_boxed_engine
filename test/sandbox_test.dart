import 'package:test/test.dart';

main() {
  test('sandbox', skip: true, () async {
    // final dictionary =
    //     await loadDictionary('assets/pt_dictionary_valid_words_only.json');

    // print(dictionary.length);
    // dictionary.removeWhere((w) => w.contains(RegExp('k|y|w')));
    // print(dictionary.length);
  });
}
